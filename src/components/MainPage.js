import React from 'react';
import HeroSection from './HeroSection';
import './MainPage.css'; // o cualquier otro archivo CSS que estés usando

function MainPage() {
    return (
        <div>
            <HeroSection />
            {/* Otros componentes */}
        </div>
    );
}

export default MainPage;
